apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: wso2-backend
  namespace: wso2-is
spec:
  endpoints:
  - fqdn:
      hostname: wso2is.example.com
      port: 443
  tls:
    insecureSkipVerify: true
